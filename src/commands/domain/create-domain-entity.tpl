import { Entity } from '../../../core';

interface {{domain}}State {
  id: number;
}

export class {{domain}}Entity extends Entity<number, {{domain}}State> {
  // Implement your domain entity logic here.
}
