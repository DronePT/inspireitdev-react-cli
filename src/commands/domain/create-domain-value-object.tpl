import { ValueObject } from '../../../core';

interface {{domain}}State {
  // Value object state
}

export class {{domain}}ValueObject extends ValueObject<{{domain}}State> {
  // Implement your domain entity logic here.
}
